export enum UserType {
  USER = 'USER',
  ADMIN = 'ADMIN',
}

export enum GenderType {
  MALE = 'Male',
  FEMALE = 'Female',
}

export enum StatusType {
  TODO = 'ToDo',
  DOING = 'Doing',
  DONE = 'Done',
}
